
pipeline {
    agent any
    
    environment {
        
        IMAGE_NAME = 'chandanbr/bigsnake10'
		
    }

    stages {

        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/brchandan23/FlaskDockerSwarm.git'
            }
        }

        
    post {
		success {
			echo "Pipeline Successful"
		}
		failure {
			echo "Pipeline Failure"
		}
		always {
			echo "Cleaning workspace"
			deleteDir()
		}
	}

}
